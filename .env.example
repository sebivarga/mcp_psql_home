# PostgreSQL connection settings
PG_HOST=192.168.1.100        # IP or hostname of your PostgreSQL server
PG_PORT=5432
PG_DATABASE=power_consumption # Your database name
PG_USER=your_db_user
PG_PASSWORD=your_db_password
PG_SSL=false                  # Set to true if PostgreSQL requires SSL

# MCP server port (must match docker-compose)
PORT=3000
